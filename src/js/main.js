import "@styles/input.css";

import { initializeMenu, initializeTab, initializeFaq, initializeContactForm } from "@js/features";

initializeMenu();
initializeTab();
initializeFaq();
initializeContactForm();